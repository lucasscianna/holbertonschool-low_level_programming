#include "main.h"

/**
 *
 *_sterlen - count the number of car in a string
 *
 *@s: the string to count
 *
 *Return: result of the numbers of the string
 */

int _strlen(char *s)
{
int count = 0;

for (; *s != '\0'; s++)
{
count++;
}
return (count);
}
